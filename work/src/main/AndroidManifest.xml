<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dc.work">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main2Activity" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".MainActivity3"></activity>
        <activity android:name=".MainActivity4"></activity>
        <!--设置launch Mode是为了，在第二个页面可以直接推出程序-->
        <!--android:launchMode="singleTask"-->
        <activity android:name=".Main2Activity2" android:launchMode="singleTask" ></activity>
    </application>
    <!--<uses-permission-->

        <!--android:name="android.permission.RESTART_PACKAGES"></uses-permission>-->
</manifest>